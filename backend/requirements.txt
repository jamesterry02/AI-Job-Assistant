# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Auth ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1

# Additional packages (OpenAI, pypdf, Azure Blob, pgvector, ...) are added to
# this file only when the feature that needs them is actually built.
