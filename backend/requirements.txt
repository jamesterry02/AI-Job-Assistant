# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# Additional packages (auth, OpenAI, pypdf, Azure Blob, pgvector, pytest, ...)
# are added to this file only when the feature that needs them is actually
# built, not in advance.
