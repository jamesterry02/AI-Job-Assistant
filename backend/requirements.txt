# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# Additional packages (SQLAlchemy/Alembic, auth, OpenAI, pypdf, Azure Blob,
# pytest, ...) are added to this file only when the feature that needs them
# is actually built, not in advance.
